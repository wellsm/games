{
  "name": "@games/games",
  "description": "Games API",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "main": "ez4.project.js",
  "private": true,
  "exports": {
    ".": "./src/export.ts"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "seed": "dotenv -e ../../local.env -- ez4 run --local -- ./src/database/seed.ts",
    "build": "tsc -p tsconfig.json",
    "generate:api-spec": "tsc -p tsconfig.json && ez4 generate -- gateway:oas ./docs",
    "serve": "dotenv -e ../../local.env -- ez4 serve --local"
  },
  "devDependencies": {
    "@clerk/backend": "^2.29.0",
    "@ez4/aws-dynamodb": "file:.yalc/@ez4/aws-dynamodb",
    "@ez4/common": "^0.36.0",
    "@ez4/database": "^0.36.0",
    "@ez4/docs-gateway-ui": "^0.36.0",
    "@ez4/gateway": "^0.36.0",
    "@ez4/local-database": "^0.36.0",
    "@ez4/local-gateway": "^0.36.0",
    "@ez4/local-queue": "^0.36.0",
    "@ez4/queue": "^0.36.0"
  },
  "dependencies": {
    "@ez4/raw-pg": "file:.yalc/@ez4/raw-pg",
    "react-query": "^3.39.3",
    "svix": "^1.82.0"
  }
}
